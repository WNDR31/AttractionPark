<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Galería de Atracciones</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0-alpha1/css/bootstrap.min.css">
    <style>
        :root {
            --darkblue: #00408C;
            --blue: #96ADD6;
            --red: #E85234;
            --peach: #F9B8AF;
            --pink: #F2D7D3;
            --background-color: white;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: white;
            font-family: "Montserrat", sans-serif;
        }

        .attraction-card {
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out;
        }

        .attraction-card:hover {
            transform: translateY(-10px);
        }

        .attraction-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            transition: transform 0.3s ease-in-out;
        }

        .attraction-card:hover img {
            transform: scale(1.05);
        }

        .card-body {
            padding: 15px;
            background-color: var(--background-color);
        }

        .card-body h5 {
            color: var(--darkblue);
            font-size: 1.25rem;
            font-weight: bold;
        }

        .card-body p {
            color: #555;
            font-size: 1rem;
            margin-bottom: 10px;
        }

        .filters {
            margin-bottom: 20px;
            padding: 15px;
            background-color: var(--blue);
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .filters select {
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid var(--darkblue);
        }

        .filters button {
            background-color: var(--darkblue);
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
        }

        .filters button:hover {
            background-color: var(--red);
        }

        .filters label {
            color: var(--darkblue);
            font-weight: bold;
        }

        .filters .row {
            margin-bottom: 15px;
        }

        .attraction-card a {
            background-color: var(--blue);
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            text-align: center;
            display: block;
            margin-top: 10px;
        }

        .attraction-card a:hover {
            background-color: var(--peach);
        }

        .text-center {
            text-align: center;
            color: var(--darkblue);
            font-size: 1.5rem;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="my-4 text-center">Galería de Atracciones</h1>

    <!-- Filtros -->
    <div class="filters">
        <form action="/attractions/filter" method="get">
            <div class="row">
                <div class="col-md-4">
                    <label for="zone" class="form-label">Zona</label>
                    <select class="form-select" id="zone" name="zone">
                        <option value="">Seleccione Zona</option>
                        {{#zones}}
                            <option value="{{name}}">{{name}}</option>
                        {{/zones}}
                    </select>
                </div>
                <div class="col-md-4">
                    <label for="intensity" class="form-label">Intensidad</label>
                    <select class="form-select" id="intensity" name="intensity">
                        <option value="">Seleccione Intensidad</option>
                        {{#intensities}}
                            <option value="{{name}}">{{name}}</option>
                        {{/intensities}}
                    </select>
                </div>
                <div class="col-md-4">
                    <label for="type" class="form-label">Tipo</label>
                    <select class="form-select" id="type" name="type">
                        <option value="">Seleccione Tipo</option>
                        {{#types}}
                            <option value="{{name}}">{{name}}</option>
                        {{/types}}
                    </select>
                </div>
            </div>
            <button type="submit" class="btn btn-primary mt-3">Aplicar Filtros</button>
            <a href="/attractionsgalery" class="btn btn-secondary mt-3 ms-2">Borrar Filtros</a>
        </form>
    </div>

    <!-- Galería de Atracciones -->
    <div class="row">
        {{#attractions}}
            <div class="col-md-4">
                <div class="attraction-card">
                    <img src="{{imageUrl1}}" alt="{{name}}">
                    <div class="card-body">
                        <h5 class="card-title">{{name}}</h5>
                        <p class="card-text">{{description}}</p>
                        <p><strong>Altura:</strong> {{height}} pies</p>
                        <p><strong>Duración:</strong> {{duration}} segundos</p>
                        <p><strong>Velocidad:</strong> {{speed}} mph</p>
                        <p><strong>Tipo:</strong> {{type.name}}</p>
                        <p><strong>Intensidad:</strong> {{intensity.name}}</p>
                        <p><strong>Zona:</strong> {{zone.name}}</p>
                        <a href="/attractions/{{id}}" class="btn btn-info">Ver más</a>
                    </div>
                </div>
            </div>
        {{/attractions}}

        {{^attractions}}
            <p class="text-center">No se encontraron atracciones que coincidan con los filtros seleccionados.</p>
        {{/attractions}}
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0-alpha1/js/bootstrap.bundle.min.js"></script>
</body>
</html>
